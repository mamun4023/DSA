<script> 



// user defined class node
class Node{
	constructor(element){
		this.element = element;
		this.next = null;
	}
}

// declare the first node
class LinkList {
	constructor(){
		this.head = null;
		this.size = 0;
	}
}

// inserting element function

 function add(element){
 	var node = new Node(element);
 	var  travarse;
 	if(this.head == null){
 		this.head = node;
 	}
 	else{
 		 travarse= this.head;
 		// go end of the list
 		while( travarse.next){
 			 travarse =  travarse.next;
 		}
 		 travarse.next  = node;
 	}

 	this.size++;
 }
 
add(30);
add(20);



// prints the list items 
function printList() 
{ 
    var travarse = this.head; 
    while (travarse) { 
        console.log(travarse.element) 
        travarse = travarse.next; 
    } 
} 

printList();

</script> 