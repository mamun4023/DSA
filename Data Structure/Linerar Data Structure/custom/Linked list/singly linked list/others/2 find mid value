    findMiddle(head) {
          if (!head) return null;

          let slow = head;
          let fast = head;

          // Move fast twice as fast as slow
          while (fast.next !== null) {
            slow = slow.next;
            fast = fast.next.next;
          }

          // slow is now at the middle
          document.write(slow.value, " ")
    }
